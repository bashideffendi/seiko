# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo

# Phase 2/3: never commit the 3GB of catalogue PDFs/scans — they live in object storage
/public/catalogue-scans/
*.pdf

# Phase 3 vision-extraction render artifacts (regenerated on demand)
/.pilot/

# local editor / dev-server config (absolute paths)
/.claude/
